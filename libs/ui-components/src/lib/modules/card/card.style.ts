import { css } from 'lit';

export const cardStyle = css`
  
  :host {
      padding: 16px;
      background-color: var(--color-background-bg-primary);
      border-radius: 32px;
      display: flex;
      flex-direction: column;
      gap: 8px;
      width: fit-content;
      height: fit-content;
  }

`