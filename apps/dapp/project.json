{
  "name": "dapp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/dapp/src",
  "tags": [],
  "targets": {
    "run:app-and-deps": {
      "executor": "nx:run-commands",
      "options": {
        "command": "concurrently --kill-others \"nx run dapp:serve\" \"nx run ui-components:watch\""
      }
    }
  }
}
